% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/entropy.R
\name{renyi_divergence}
\alias{renyi_divergence}
\title{Renyi Cross-Entropy Divergence of Order Alpha}
\usage{
renyi_divergence(p, q, alpha, base = exp(1))
}
\arguments{
\item{p}{Numeric vector. The estimated distribution.}

\item{q}{Numeric vector. The reference (prior) distribution.}

\item{alpha}{Numeric scalar. Order parameter (positive, not equal to 1).}

\item{base}{Positive numeric. Logarithm base (default: natural log).}
}
\value{
A single numeric value.
}
\description{
Computes the Renyi cross-entropy (divergence) of order alpha:
D^R_alpha(p || q) = (1 / (1 - alpha)) * log(sum(p^alpha * q^(1-alpha))).
Reduces to the KL divergence as alpha -> 1.
}
\examples{
p <- c(0.5, 0.3, 0.2)
q <- rep(1/3, 3)
renyi_divergence(p, q, alpha = 2)

}
\references{
Golan, A. (2008). Information and Entropy Econometrics. \emph{Foundations
and Trends in Econometrics}, \strong{2}(1-2), 1-145. Eq. (3.6).
}
