% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix_ce.R
\name{matrix_ce}
\alias{matrix_ce}
\alias{coef.matrix_ce}
\alias{fitted.matrix_ce}
\alias{residuals.matrix_ce}
\alias{print.matrix_ce}
\alias{summary.matrix_ce}
\alias{print.summary.matrix_ce}
\title{Cross-Entropy Matrix Balancing}
\usage{
matrix_ce(y, x, p0 = NULL, control = list())

\method{coef}{matrix_ce}(object, ...)

\method{fitted}{matrix_ce}(object, ...)

\method{residuals}{matrix_ce}(object, ...)

\method{print}{matrix_ce}(x, digits = max(3L, getOption("digits") - 3L), ...)

\method{summary}{matrix_ce}(object, ...)

\method{print}{summary.matrix_ce}(x, digits = max(3L, getOption("digits") - 3L), ...)
}
\arguments{
\item{y}{Numeric vector of length n: the row-weighted aggregates
\eqn{y_i = \sum_j p_{ij} x_j}.}

\item{x}{Numeric vector of length m: the column weights (in \code{print},
the fitted \code{matrix_ce} object).}

\item{p0}{Optional n-by-m matrix of strictly-positive prior probabilities
whose columns sum to 1. Defaults to uniform (\code{1/n}).}

\item{control}{A named list merged over the defaults and passed to
\code{\link[stats]{optim}} (BFGS): \code{maxit} (default 500) and
\code{reltol} (default 1e-12).}

\item{object}{A \code{matrix_ce} object.}

\item{...}{Additional arguments passed to or from methods (currently ignored).}

\item{digits}{Number of significant digits to print.}
}
\value{
An object of class \code{c("matrix_ce", "infometrics")}, which is a list
containing the following components:
\describe{
  \item{\code{p}, \code{p_hat}}{n-by-m matrix of estimated column-stochastic
    probabilities (two names for the same object). Extracted by
    \code{\link{coef}}.}
  \item{\code{lambda}, \code{lambda_hat}}{Numeric vector of length n: the
    estimated row multipliers (two names for the same object).}
  \item{\code{fitted}, \code{fitted.values}}{Numeric vector of length n:
    \eqn{\sum_j \hat p_{ij} x_j}{sum_j p-hat_ij x_j} (two names for the same
    object). Extracted by \code{\link{fitted}}.}
  \item{\code{residuals}}{Numeric vector of length n:
    \eqn{y - \hat y}{y - y-hat}, the row-aggregate residual. Extracted by
    \code{\link{residuals}}.}
  \item{\code{entropy}, \code{H_signal}}{Single number: the Shannon entropy
    \eqn{H(\hat p)}{H(p-hat)} (two names for the same value).}
  \item{\code{entropy_p0}}{Single number: the prior entropy \eqn{H(p_0)}{H(p0)}.}
  \item{\code{S}}{Single number: the prior-relative normalized entropy
    \eqn{H(\hat p)/H(p_0)}{H(p-hat)/H(p0)}, which lies in [0, 1] for a
    uniform \code{p0}.}
  \item{\code{cross_entropy}}{Single number: the cross-entropy
    \eqn{D(\hat p\|p_0)}{D(p-hat || p0)} at the optimum.}
  \item{\code{objective}, \code{value}}{Single number: the minimised dual
    objective (two names for the same value).}
  \item{\code{prior}}{The resolved n-by-m prior \code{p0}.}
  \item{\code{converged}}{Logical: \code{TRUE} when \code{optim} reported
    convergence.}
  \item{\code{convergence}}{Integer: the raw \code{\link[stats]{optim}}
    convergence code.}
  \item{\code{method}}{Character string naming the solver, \code{"dual"}.}
  \item{\code{y}, \code{x}, \code{n}, \code{m}}{The resolved inputs and their
    lengths \code{n = length(y)}, \code{m = length(x)}.}
  \item{\code{call}}{The matched call.}
}
}
\description{
Recovers a non-negative matrix \eqn{p} (each column a probability
distribution) from column weights \code{x} and row-weighted aggregates
\code{y} by minimizing the cross-entropy \eqn{D(p\|p0)} relative to a prior
\code{p0}, solving the concentrated dual of Golan (2008), Section 7.2. With
a uniform \code{p0} this reduces to Maximum Entropy. The model is
\deqn{y_i = \sum_j p_{ij} x_j, \qquad \sum_i p_{ij} = 1,}
with solution \eqn{\hat p_{ij} = p0_{ij}\exp(\hat\lambda_i x_j)/\Omega_j} and
\eqn{\Omega_j(\lambda) = \sum_i p0_{ij}\exp(\lambda_i x_j)}.
}
\details{
The dual is minimised here (convex) to match the package convention used by
\code{\link{inverse_ce}}:
\deqn{\ell(\lambda) = -\sum_i \lambda_i y_i + \sum_j \log\Omega_j(\lambda),}
minimised over the \eqn{n} row multipliers (equivalent to maximising the
column entropies subject to the data). The problem is typically
under-determined (\eqn{n\times m} unknowns, \eqn{n + m} constraints), so the
returned matrix is the minimum cross-entropy matrix relative to \code{p0}
consistent with the data (the maximum-entropy matrix when \code{p0} is
uniform) -- not necessarily the matrix that generated \code{y}. To bias the
solution toward a known target, pass that target as \code{p0}.

The normalized entropy is \eqn{S = H(\hat p) / H(p0)} (Golan §7.5), the ratio
of the total Shannon entropy to the prior's entropy. For a uniform \code{p0}
(Maximum Entropy) \eqn{H(p0) = m\log n} (the absolute maximum, \eqn{m} columns
uniform over \eqn{n} rows), so \eqn{S \in [0, 1]} with \eqn{S = 1} when every
column is uniform; for a non-uniform prior \eqn{S} is relative to that prior.
\code{summary()} reports the §7.5 information measures built on \eqn{S}: the
information index \eqn{I = 1 - S}, pseudo-\eqn{R^2 = 1 - S}, the per-column
normalized entropy \eqn{S(p_j) = H(p_j)/H(p0_j)}, and the entropy-ratio test
\eqn{W = 2[H(p0) - H(\hat p)] \sim \chi^2_{n-1}} for \eqn{H_0\!: P = P0}.
}
\section{Methods (by generic)}{
\itemize{
\item \code{coef(matrix_ce)}: Estimated probability matrix \eqn{\hat p} (n x m).

\item \code{fitted(matrix_ce)}: Fitted row aggregates \eqn{\sum_j p_{ij} x_j}.

\item \code{residuals(matrix_ce)}: Moment residuals \eqn{y -} fitted.

\item \code{print(matrix_ce)}: Compact printed display.

\item \code{summary(matrix_ce)}: Summary with the Section-7.5 information measures, the
entropy-ratio test for the signal matrix, and a moment-fit table.

}}
\section{Functions}{
\itemize{
\item \code{print(summary.matrix_ce)}: Print method for the summary object.

}}
\examples{
P <- sweep(matrix(1:6, ncol = 2), 2, colSums(matrix(1:6, ncol = 2)), "/")
x <- c(3, 2)
y <- as.vector(P \%*\% x)
fit <- matrix_ce(y, x)
fit$p
summary(fit)

}
\references{
Golan, A. (2008). \emph{Information and Entropy Econometrics -
  A Review and Synthesis}. Foundations and Trends in Econometrics,
  2(1-2), 1-145. Section 7.2.
}
\seealso{
\code{\link{inverse_ce}} for the formula-interface
  pure-moment ME/CE estimator.
}
