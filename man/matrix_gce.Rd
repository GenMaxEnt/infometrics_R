% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix_gce.R
\name{matrix_gce}
\alias{matrix_gce}
\alias{coef.matrix_gce}
\alias{fitted.matrix_gce}
\alias{residuals.matrix_gce}
\alias{print.matrix_gce}
\alias{summary.matrix_gce}
\alias{print.summary.matrix_gce}
\title{Generalized Cross-Entropy Matrix Balancing (Stochastic Moments)}
\usage{
matrix_gce(y, x, p0 = NULL, w0 = NULL, v = NULL, nu = 0.5, control = list())

\method{coef}{matrix_gce}(object, ...)

\method{fitted}{matrix_gce}(object, ...)

\method{residuals}{matrix_gce}(object, ...)

\method{print}{matrix_gce}(x, digits = max(3L, getOption("digits") - 3L), ...)

\method{summary}{matrix_gce}(object, ...)

\method{print}{summary.matrix_gce}(x, digits = max(3L, getOption("digits") - 3L), ...)
}
\arguments{
\item{y, x}{Numeric vectors (lengths n and m), each normalized to [0, 1] so
the implied errors lie in [-1, 1].}

\item{p0}{Optional n-by-m signal prior, columns summing to 1, strictly
positive. Default uniform.}

\item{w0}{Optional n-by-H noise prior, rows summing to 1, strictly positive.
Default uniform; its column count must match \code{length(v)}.}

\item{v}{Noise support, symmetric around 0 in [-1, 1]: \code{NULL} (default
\code{c(-1, 0, 1)}), a single whole number read as a support-point count
\code{H >= 2}, or an explicit numeric vector (used as given).}

\item{nu}{Entropy weight in \eqn{(0, 1)}; default 0.5.}

\item{control}{A named list merged over the defaults and passed to
\code{\link[stats]{optim}} (BFGS): \code{maxit} (default 500) and
\code{reltol} (default 1e-12).}

\item{object}{A \code{matrix_gce} object (\code{x} in \code{print}).}

\item{...}{Additional arguments passed to or from methods (currently ignored).}

\item{digits}{Significant digits to print.}
}
\value{
An object of class \code{c("matrix_gce", "infometrics")}, which is a list
containing the following components:
\describe{
  \item{\code{p}, \code{p_hat}}{n-by-m matrix of estimated column-stochastic
    signal probabilities (two names for the same object). Extracted by
    \code{\link{coef}}.}
  \item{\code{w}, \code{w_hat}}{n-by-H matrix of estimated row-stochastic
    noise probabilities (two names for the same object).}
  \item{\code{e}}{Numeric vector of length n: the estimated noise,
    \eqn{e_i = \sum_h v_h \hat w_{ih}}{e_i = sum_h v_h w-hat_ih}.}
  \item{\code{lambda}, \code{lambda_hat}}{Numeric vector of length n: the
    estimated row multipliers (two names for the same object).}
  \item{\code{nu}}{Single number: the entropy weight used.}
  \item{\code{fitted.values}}{Numeric vector of length n: \eqn{\hat P x}{P-hat x}.
    Extracted by \code{\link{fitted}}.}
  \item{\code{residuals}}{Numeric vector of length n:
    \eqn{y - \hat P x}{y - P-hat x}, which equals \code{e} at a converged
    optimum. Extracted by \code{\link{residuals}}.}
  \item{\code{entropy_p}, \code{H_signal}}{Single number: the signal entropy
    \eqn{H(\hat p)}{H(p-hat)} (two names for the same value).}
  \item{\code{entropy_w}}{Single number: the noise entropy
    \eqn{H(\hat w)}{H(w-hat)}.}
  \item{\code{entropy_p0}, \code{entropy_w0}}{Single numbers: the signal and
    noise prior entropies \eqn{H(p_0)}{H(p0)} and \eqn{H(w_0)}{H(w0)}.}
  \item{\code{S_p}, \code{S}}{Single number: the normalized signal entropy
    \eqn{H(\hat p)/H(p_0)}{H(p-hat)/H(p0)} (two names for the same value).}
  \item{\code{S_w}}{Single number: the normalized noise entropy
    \eqn{H(\hat w)/H(w_0)}{H(w-hat)/H(w0)}.}
  \item{\code{objective}, \code{value}}{Single number: the minimised dual
    objective (two names for the same value).}
  \item{\code{signal_prior}, \code{noise_prior}}{The resolved priors
    \code{p0} (n-by-m) and \code{w0} (n-by-H).}
  \item{\code{support}}{Numeric vector of length H: the resolved noise
    support \code{v}.}
  \item{\code{converged}}{Logical: \code{TRUE} when \code{optim} reported
    convergence.}
  \item{\code{convergence}}{Integer: the raw \code{\link[stats]{optim}}
    convergence code.}
  \item{\code{method}}{Character string naming the solver, \code{"dual"}.}
  \item{\code{call}}{The matched call.}
}
}
\description{
Noisy extension of \code{\link{matrix_ce}} (Golan 2008, Section 7.4). The
aggregates are treated as stochastic, \eqn{y_i = \sum_j p_{ij} x_j + e_i}
with \eqn{e_i = \sum_h v_h w_{ih}}, and the cross-entropy of both the signal
\code{p} (columns sum to 1) and the noise \code{w} (rows sum to 1) is
minimized relative to priors \code{p0}, \code{w0}.
}
\details{
The dual is minimised here (convex) to match the package convention used by
\code{\link{inverse_noise}} and \code{\link{matrix_ce}}:
\deqn{
  \ell(\lambda) = -\sum_i \lambda_i y_i + \nu \sum_j \log\Omega_j(\lambda)
       + (1-\nu)\sum_i \log\Psi_i(\lambda),
}
minimised over the \eqn{n} row multipliers, with
\eqn{\hat p_{ij} \propto p0_{ij}\exp(\lambda_i x_j/\nu)} (normalised within
each column) and \eqn{\hat w_{ih} \propto w0_{ih}\exp(\lambda_i v_h/(1-\nu))}
(normalised within each row); \eqn{\hat e_i = \sum_h v_h \hat w_{ih}}.
Equivalent to maximising the joint signal-plus-noise entropy subject to the
stochastic-moment constraints.

The weight \eqn{\nu \in (0,1)} (an extension to Eq. 7.22) balances prediction
against precision by trading signal entropy against noise entropy. At
\eqn{\nu = 0.5} this is the equal-weight GCE of Eq. (7.22) and the estimates
coincide with it (only the \eqn{\lambda} scale differs). As the noise support
\code{v} shrinks toward 0 the noise is forced out and the solution returns to
\code{\link{matrix_ce}}.
}
\section{Methods (by generic)}{
\itemize{
\item \code{coef(matrix_gce)}: Estimated signal matrix \code{p}.

\item \code{fitted(matrix_gce)}: Fitted aggregates \eqn{Px}.

\item \code{residuals(matrix_gce)}: Residuals \eqn{y - Px} (the estimated noise \code{e}).

\item \code{print(matrix_gce)}: Compact display.

\item \code{summary(matrix_gce)}: Summary with the Section-7.5 information measures
and entropy-ratio test for the signal matrix.

}}
\section{Functions}{
\itemize{
\item \code{print(summary.matrix_gce)}: Print method for the summary object.

}}
\examples{
set.seed(1)
P <- matrix(runif(6), ncol = 2); P <- sweep(P, 2, colSums(P), "/")
x <- c(0.6, 0.3); y <- as.vector(P \%*\% x)
fit <- matrix_gce(y, x, nu = 0.5)
coef(fit)        # estimated signal matrix p
residuals(fit)   # y - Px  (= estimated noise e)
summary(fit)

}
\references{
Golan, A. (2008). \emph{Information and Entropy Econometrics -
  A Review and Synthesis}. Foundations and Trends in Econometrics,
  2(1-2), 1-145. Section 7.4.
}
\seealso{
\code{\link{matrix_ce}} (the exact-moment version this reduces to as
  \code{v} shrinks to 0).
}
